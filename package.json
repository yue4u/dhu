{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna test",
    "build": "lerna build",
    "release": "lerna publish --no-verify-access",
    "version": "lerna version",
    "dev:cli": "yarn workspace @dhu/cli dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^13.13.30",
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "chalk": "^4.0.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.8.3"
  }
}
