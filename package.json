{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna test",
    "build": "lerna build",
    "release": "lerna publish --no-verify-access",
    "version": "lerna version",
    "dev:cli": "yarn workspace @dhu/cli dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^14.14.37",
    "lerna": "^4.0.0"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "chalk": "^4.1.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
